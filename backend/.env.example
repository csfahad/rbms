# =================================
# RAILWAY BOOKING SYSTEM - ENVIRONMENT VARIABLES
# =================================

# =================================
# DATABASE CONFIGURATION
# =================================
DATABASE_URL=postgresql://username:password@host:port/database_name

# =================================
# SERVER CONFIGURATION
# =================================
PORT=5000
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-here

# =================================
# EMAIL CONFIGURATION
# =================================

# Choose your email provider: gmail, sendgrid, mailgun, outlook, resend, custom
EMAIL_PROVIDER=resend

# Set to true to skip email sending in development (OTP will be logged to console)
SKIP_EMAIL_SENDING=true

# =================================
# RESEND CONFIGURATION (EMAIL_PROVIDER=resend) - RECOMMENDED
# =================================
# 1. Create account at https://resend.com/
# 2. Add and verify your domain
# 3. Create API Key in Settings > API Keys
RESEND_API_KEY=re_your-resend-api-key-here

# =================================
# GMAIL CONFIGURATION (EMAIL_PROVIDER=gmail)
# =================================
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the App Password, NOT your regular Gmail password
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# =================================
# SENDGRID CONFIGURATION (EMAIL_PROVIDER=sendgrid)
# =================================
# 1. Create account at https://sendgrid.com/
# 2. Create API Key in Settings > API Keys
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# =================================
# MAILGUN CONFIGURATION (EMAIL_PROVIDER=mailgun)
# =================================
# 1. Create account at https://www.mailgun.com/
# 2. Get SMTP credentials from your domain dashboard
MAILGUN_SMTP_LOGIN=postmaster@your-domain.mailgun.org
MAILGUN_SMTP_PASSWORD=your-mailgun-smtp-password

# =================================
# OUTLOOK/HOTMAIL CONFIGURATION (EMAIL_PROVIDER=outlook)
# =================================
OUTLOOK_USER=your-email@outlook.com
OUTLOOK_PASSWORD=your-outlook-password

# =================================
# CUSTOM SMTP CONFIGURATION (EMAIL_PROVIDER=custom)
# =================================
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# =================================
# EMAIL SETTINGS
# =================================
FROM_EMAIL="RailBuddy" <noreply@yourdomain.com>

# =================================
# INSTRUCTIONS FOR PRODUCTION
# =================================
# 1. Set NODE_ENV=production
# 2. Set SKIP_EMAIL_SENDING=false (or remove this line)
# 3. Choose your email provider and configure the respective credentials
# 4. Set a strong JWT_SECRET
# 5. Configure your production database URL